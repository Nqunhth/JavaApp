<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_courseadviser"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_courseadviser"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myapp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".controller.activity.LoginActivity">
        </activity>

        <activity
            android:name=".controller.activity.ForgetActivity">
        </activity>

        <activity android:name=".controller.activity.MainActivity"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
</manifest>